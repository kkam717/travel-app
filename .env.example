# Copy to .env and fill in your Supabase credentials (never commit .env).
# Security: No hardcoded keys; rotate keys if exposed. Use anon key only; never service_role in client apps.
# Get these from your Supabase project: Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# Auth redirect for email verification (hosted auth_redirect page - works on mobile & PC)
# Deploy auth_redirect/ to Vercel/Netlify (Root=auth_redirect), then set e.g. SUPABASE_AUTH_REDIRECT_URL=https://your-site.vercel.app

# Optional: Temporary developer sign-in (create this user in Supabase Auth first)
# DEV_EMAIL=dev@example.com
# DEV_PASSWORD=devpassword123

# --- Static maps (feed card thumbnails) ---
# Geoapify Static Maps (requires GEOAPIFY_API_KEY)
# Get from https://www.geoapify.com/ (free tier: 3,000 credits/day)
# GEOAPIFY_API_KEY=

# Place search, geocoding, map links: Photon (OSM) + Nominatim (free, no API key)

# Classic picks (suggested cities by country): GeoNames (free, no API key; username only)
# Get a free username at https://www.geonames.org/login — enables Rome, Milan, Lake Como–style suggestions
# GEONAMES_USERNAME=

# --- Firebase App Distribution (for beta testing) ---
# Get these from Firebase Console → Project Settings → Your apps
# Android App ID looks like: 1:123456789:android:abc123def456
# iOS App ID looks like: 1:123456789:ios:abc123def456
# ANDROID_APP_ID=
# IOS_APP_ID=
